"use client";
// L3 User Management — staff lifecycle management (requires L4 approval for changes)
import { L3Dashboard } from "@/components/admin/l3-dashboard";

export default function L3UsersPage() {
    return <L3Dashboard />;
}
